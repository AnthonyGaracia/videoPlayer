import React, { useEffect, useRef } from 'react';
import videojs from 'vide.js';
import 'video.js/dist/video-js.css';

function VideoJs({ options}) {
   
    const videoRef = useRef(null);
    const playerRef = useRef(null);
    
    
  
    return (
     <div>
  
     </div>
    )
  }
  
  export default VideoJs